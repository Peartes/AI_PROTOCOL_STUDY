[package]
name = "macro_tester"
version = "0.1.0"
edition = "2021"

[dependencies]
proc_macros = { path = "../proc_macros" }
syn = {version = "2", features = ["full"]}
quote = { version = "1"}
proc-macro2 ={ version = "1", default-features = false}
serde_json = {version = "*"}